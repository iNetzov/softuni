function solve(first, second, third){
    let strLength = first.length + third.length + second.length;
    console.log(strLength);
    let avg = Math.floor(strLength/3)
    console.log(avg);
}

solve('chocolate','ice cream' , 'cake');
